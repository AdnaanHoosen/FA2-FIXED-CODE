# **TASK 4: NON-FUNCTIONAL TESTING (FULL TABULAR REPORT)**

| **Test Type** | **Describe Method** | **Record Results** | **Interpret Findings** |
| :--- | :--- | :--- | :--- |
| **Performance Testing** | Used a digital stopwatch to measure "Response Latency" for report generation. Performed 5 consecutive runs to verify consistency. | **Result:** Average loading time was **4.02 seconds**. The delay was identical across every run. | **FINDING:** Sub-optimal. Code analysis shows an intentional `Thread.Sleep(4000)`. This is a hardcoded bottleneck that artificially slows down the app. |
| **Usability Testing** | Performed "Path-Click Analysis" to count the number of interactions required to navigate from the Login screen to the Marks Capture form. | **Result:** **2 Clicks** and **2 field entries**. The navigation path followed a clear hierarchical tree (Login -> Dashboard -> Module). | **FINDING:** Good. The system is intuitive for new users. However, it lacks advanced navigation (like a sidebar) which would be needed for higher efficiency. |
| **Security Testing** | Employed "Logic Bypass" testing by entering a known password ("1234") with an unauthorized username to test authentication strictness. | **Result:** **Bypass Successful**. The system granted access to the Dashboard despite the incorrect username. | **FINDING:** Critical Failure. The code uses an `OR` logic gate instead of `AND`. This allows unauthorized users to gain access easily, posing a major security risk. |
| **Reliability Testing** | Used "Fault Injection" by repeatedly entering alphabetic text into numeric "Marks" fields to see if the system recovers from user errors. | **Result:** **100% Failure Rate**. The application crashed immediately with a `System.FormatException` on every attempt. | **FINDING:** Poor. The system lacks fault tolerance and robust error handling. It is unreliable because a single typo forces a complete system restart. |
